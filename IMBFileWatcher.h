//----------------------------------------------------------------------------------------------------------------------


// This is a wrapper file that renames Uli's UK classes to have the prefix IMB. That way we can use these 
// classes without running into runtime conflicts with other UKKQueue classes being loaded by the runtime 
// from other bundles. The preprocessor technique qas suggested by Uli himself...


#include "UK_IMB_RenameBegin.h" 
#include "UKFileWatcher.h" 
#include "UK_IMB_RenameEnd.h" 


//----------------------------------------------------------------------------------------------------------------------
